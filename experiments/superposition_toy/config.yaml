## Default config for the toy superposition experiment
# Values mirror ExperimentConfig in run_experiment.py.

n_features: 80
hidden_dim: 20
n_samples: 5000
sparsity_levels: [0.9, 0.7, 0.5, 0.3, 0.1]
importance_decay: 0.9
bias: true
lr: 0.005
epochs: 2000
seed: 123
out_dir: experiments/superposition_toy/runs
